import react from 'react';

const Input = () => {
    return (
        <div>
            <input placeholder={'test'} />

            {'testestest'}
            
        </div>

    );
};

export const Test = () => {
    return <div>
        sestsetestest
    </div>;
}

export default Input;